export interface TravelElement {
    name: string;
    email: string;
    location: string;
    num_of_travellers: string;
    budget_per_person: string;
}

export interface LocationElement {
    id: number;
    name: string;
}